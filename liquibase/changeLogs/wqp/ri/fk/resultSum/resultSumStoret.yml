databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql

  - changeSet:
      author: drsteini
      id: "create.${schemaName}.result_sum_storet_station_fk"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - foreignKeyConstraintExists:
              schemaName: ${schemaName}
              foreignKeyName: result_sum_storet_station_fk
      changes:
        - sql: alter table ${schemaName}.result_sum_storet add constraint result_sum_storet_station_fk foreign key (data_source_id, station_id) references ${schemaName}.station_sum_storet (data_source_id, station_id);
        - rollback: alter table ${schemaName}.result_sum_storet drop constraint result_sum_storet_station_fk;
