databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql

  - changeSet:
      author: drsteini
      id: "create.${schemaName}.activity_stewards_station_fk"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - foreignKeyConstraintExists:
              schemaName: ${schemaName}
              foreignKeyName: activity_stewards_station_fk
      changes:
        - sql: alter table ${schemaName}.activity_stewards add constraint activity_stewards_station_fk foreign key (data_source_id, station_id) references ${schemaName}.station_stewards (data_source_id, station_id);
        - rollback: alter table ${schemaName}.activity_stewards drop constraint if exists activity_stewards_station_fk;
