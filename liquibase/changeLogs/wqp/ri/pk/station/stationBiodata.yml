databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql

  - changeSet:
      author: drsteini
      id: "create.${schemaName}.station_biodata_pk"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - primaryKeyExists:
              schemaName: ${schemaName}
              tableName: station_biodata
      changes:
        - sql: alter table ${schemaName}.station_biodata add constraint station_biodata_pk primary key (data_source_id, station_id) with (fillfactor = 100);
        - rollback: alter table ${schemaName}.station_biodata drop constraint if exists station_biodata_pk;
