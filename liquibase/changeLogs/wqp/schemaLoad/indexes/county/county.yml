databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql

  - changeSet:
      author: drsteini
      id: "create.index.${schemaName}.us_counties_dis_20121015_geom"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              schemaName: ${schemaName}
              indexName: us_counties_dis_20121015_geom
      changes:
        - sql: create index if not exists us_counties_dis_20121015_geom on ${schemaName}.us_counties_dis_20121015 using gist (geom) with (fillfactor = 100);
        - rollback: drop index if exists ${schemaName}.us_counties_dis_20121015_geom;
