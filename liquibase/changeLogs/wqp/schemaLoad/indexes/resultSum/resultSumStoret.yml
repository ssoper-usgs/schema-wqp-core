databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_analytical"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_analytical
      changes:
        - sql: create index if not exists result_sum_storet_analytical on ${WQP_SCHEMA_NAME}.result_sum_storet(analytical_method) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_analytical;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_assemblage"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_assemblage
      changes:
        - sql: create index if not exists result_sum_storet_assemblage on ${WQP_SCHEMA_NAME}.result_sum_storet(assemblage_sampled_name) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_assemblage;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_char_name"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_char_name
      changes:
        - sql: create index if not exists result_sum_storet_char_name on ${WQP_SCHEMA_NAME}.result_sum_storet(characteristic_name) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_char_name;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_char_type"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_char_type
      changes:
        - sql: create index if not exists result_sum_storet_char_type on ${WQP_SCHEMA_NAME}.result_sum_storet(characteristic_type) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_char_type;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_country"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_country
      changes:
        - sql: create index if not exists result_sum_storet_country on ${WQP_SCHEMA_NAME}.result_sum_storet(substring(governmental_unit_code, '[^:]+')) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_country;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_county"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_county
      changes:
        - sql: create index if not exists result_sum_storet_county on ${WQP_SCHEMA_NAME}.result_sum_storet(substring(governmental_unit_code, '[^:]+:[^:]+:[^:]+')) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_county;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_event_date"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_event_date
      changes:
        - sql: create index if not exists result_sum_storet_event_date on ${WQP_SCHEMA_NAME}.result_sum_storet(event_date) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_event_date;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_geom"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_geom
      changes:
        - sql: create index if not exists result_sum_storet_geom on ${WQP_SCHEMA_NAME}.result_sum_storet using gist (geom) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_geom;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_huc10"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_huc10
      changes:
        - sql: create index if not exists result_sum_storet_huc10 on ${WQP_SCHEMA_NAME}.result_sum_storet(substring(huc, '[0-9]{10}')) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_huc10;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_huc12"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_huc12
      changes:
        - sql: create index if not exists result_sum_storet_huc12 on ${WQP_SCHEMA_NAME}.result_sum_storet(substring(huc, '[0-9]{12}')) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_huc12;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_huc2"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_huc2
      changes:
        - sql: create index if not exists result_sum_storet_huc2 on ${WQP_SCHEMA_NAME}.result_sum_storet(substring(huc, '[0-9]{2}')) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_huc2;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_huc4"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_huc4
      changes:
        - sql: create index if not exists result_sum_storet_huc4 on ${WQP_SCHEMA_NAME}.result_sum_storet(substring(huc, '[0-9]{4}')) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_huc4;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_huc6"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_huc6
      changes:
        - sql: create index if not exists result_sum_storet_huc6 on ${WQP_SCHEMA_NAME}.result_sum_storet(substring(huc, '[0-9]{6}')) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_huc6;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_huc8"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_huc8
      changes:
        - sql: create index if not exists result_sum_storet_huc8 on ${WQP_SCHEMA_NAME}.result_sum_storet(substring(huc, '[0-9]{8}')) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_huc8;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_organization"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_organization
      changes:
        - sql: create index if not exists result_sum_storet_organization on ${WQP_SCHEMA_NAME}.result_sum_storet(organization) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_organization;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_p_code"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_p_code
      changes:
        - sql: create index if not exists result_sum_storet_p_code on ${WQP_SCHEMA_NAME}.result_sum_storet(p_code) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_p_code;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_project"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_project
      changes:
        - sql: create index if not exists result_sum_storet_project on ${WQP_SCHEMA_NAME}.result_sum_storet(project_id) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_project;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_sample_media"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_sample_media
      changes:
        - sql: create index if not exists result_sum_storet_sample_media on ${WQP_SCHEMA_NAME}.result_sum_storet(sample_media) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_sample_media;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_site"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_site
      changes:
        - sql: create index if not exists result_sum_storet_site on ${WQP_SCHEMA_NAME}.result_sum_storet(site_id) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_site;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_site_type"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_site_type
      changes:
        - sql: create index if not exists result_sum_storet_site_type on ${WQP_SCHEMA_NAME}.result_sum_storet(site_type) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_site_type;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_state"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_state
      changes:
        - sql: create index if not exists result_sum_storet_state on ${WQP_SCHEMA_NAME}.result_sum_storet(substring(governmental_unit_code, '[^:]+:[^:]+')) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_state;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_station"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_station
      changes:
        - sql: create index if not exists result_sum_storet_station on ${WQP_SCHEMA_NAME}.result_sum_storet(station_id) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_station;

  - changeSet:
      author: drsteini
      id: "create.index.${WQP_SCHEMA_NAME}.result_sum_storet_taxa_name"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              WQP_SCHEMA_NAME: ${WQP_SCHEMA_NAME}
              indexName: result_sum_storet_taxa_name
      changes:
        - sql: create index if not exists result_sum_storet_taxa_name on ${WQP_SCHEMA_NAME}.result_sum_storet(sample_tissue_taxonomic_name) with (fillfactor = 100);
        - rollback: drop index if exists ${WQP_SCHEMA_NAME}.result_sum_storet_taxa_name;
