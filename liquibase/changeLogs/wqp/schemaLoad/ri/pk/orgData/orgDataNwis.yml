databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql

  - changeSet:
      author: drsteini
      id: "create.${schemaName}.org_data_nwis_pk"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - primaryKeyExists:
              schemaName: ${schemaName}
              tableName: org_data_nwis
      changes:
        - sql: alter table ${schemaName}.org_data_nwis add constraint org_data_nwis_pk primary key (data_source_id, organization_id) with (fillfactor = 100);
        - rollback: alter table ${schemaName}.org_data_nwis drop constraint if exists org_data_nwis_pk;
