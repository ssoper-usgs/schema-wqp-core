databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql

  - changeSet:
      author: drsteini
      id: "create.index.${schemaName}.result_object_biodata_organization"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              schemaName: ${schemaName}
              indexName: result_object_biodata_organization
      changes:
        - sql: create index if not exists result_object_biodata_organization on ${schemaName}.result_object_biodata(organization) with (fillfactor = 100);
        - rollback: drop index if exists ${schemaName}.result_object_biodata_organization;

  - changeSet:
      author: drsteini
      id: "create.index.${schemaName}.result_object_biodata_activity"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              schemaName: ${schemaName}
              indexName: result_object_biodata_activity
      changes:
        - sql: create index if not exists result_object_biodata_activity on ${schemaName}.result_object_biodata(activity) with (fillfactor = 100);
        - rollback: drop index if exists ${schemaName}.result_object_biodata_activity;

  - changeSet:
      author: drsteini
      id: "create.index.${schemaName}.result_object_biodata_source"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              schemaName: ${schemaName}
              indexName: result_object_biodata_source
      changes:
        - sql: create index if not exists result_object_biodata_source on ${schemaName}.result_object_biodata(data_source) with (fillfactor = 100);
        - rollback: drop index if exists ${schemaName}.result_object_biodata_source;

  - changeSet:
      author: drsteini
      id: "create.index.${schemaName}.result_object_biodata_result"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - indexExists:
              schemaName: ${schemaName}
              indexName: result_object_biodata_result
      changes:
        - sql: create index if not exists result_object_biodata_result on ${schemaName}.result_object_biodata(result_id) with (fillfactor = 100);
        - rollback: drop index if exists ${schemaName}.result_object_biodata_result;
