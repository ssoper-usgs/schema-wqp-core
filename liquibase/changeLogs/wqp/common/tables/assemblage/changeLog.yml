databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql

  - changeSet:
      author: drsteini
      id: "create.table.${schemaName}.assemblage"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: assemblage
      changes:
        - sqlFile:
            path: assemblage.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists ${schemaName}.assemblage cascade;

  - changeSet:
      author: drsteini
      id: "create.table.${schemaName}.assemblage.partition.biodata"
      context: external
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: assemblage_biodata
      changes:
        - sqlFile:
            path: assemblageBiodata.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists ${schemaName}.assemblage_biodata cascade;

  - changeSet:
      author: drsteini
      id: "create.table.${schemaName}.assemblage.partition.nwis"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: assemblage_nwis
      changes:
        - sqlFile:
            path: assemblageNwis.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists ${schemaName}.assemblage_nwis cascade;

  - changeSet:
      author: drsteini
      id: "create.table.${schemaName}.assemblage.partition.stewards"
      context: external
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: assemblage_stewards
      changes:
        - sqlFile:
            path: assemblageStewards.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists ${schemaName}.assemblage_stewards cascade;

  - changeSet:
      author: drsteini
      id: "create.table.${schemaName}.assemblage.partition.storet"
      context: external
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: assemblage_storet
      changes:
        - sqlFile:
            path: assemblageStoret.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists ${schemaName}.assemblage_storet cascade;
