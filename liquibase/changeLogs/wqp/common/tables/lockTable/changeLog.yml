databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql

  - changeSet:
      author: drsteini
      id: "create.table.${schemaName}.lock_table"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: lock_table
      changes:
        - sqlFile:
            path: lockTable.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists ${schemaName}.lock_table cascade;

  - changeSet:
      author: drsteini
      id: "populate.table.lock_table"
      changes:
        - sql:
            sql: insert into ${schemaName}.lock_table (lock_value) values (1);
        - rollback: delete from ${schemaName}.lock_table where lock_value = 1;
